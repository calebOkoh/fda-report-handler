export interface Complaint {
  selectedDeliveryMode: any;
  name: string;
  manifacturer: string;
  dateUsed: string;
  lotNumber: string;
  description: string;
  phone: string;
  sideEffects: string;
}
